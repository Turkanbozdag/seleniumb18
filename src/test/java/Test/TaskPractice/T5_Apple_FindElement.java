package Test.TaskPractice;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
/*
import java.util.List;

public class T5_Apple_FindElement {
    public static void main(String[] args) {
        //C #03: FINDELEMENTS_APPLE
        // 1.Open Chrome browser
        // 2.Go to https://www.apple.com
        // 3.Click to all of the headers one by onea.Mac, iPad, iPhone, Watch, TV, Music, Support
        List<WebElement>appleHeader=driver.finElements(By.xpath("//ul[@class='ac-gn-list']/li/a"));
        //the loop where we specific where to start from and where to end in appleHeader list
        for (int i=1;i<8;i++){
            appleHeader.get(i).click();
            Thread.sleep(1000);
            List<WebElement>listOfLinks=driver.findElements(By.xpath("//body//a"));
        }
        // 4.Printout how many links on each pagewiththe titles of the pages
        System.out.println("Number of links on page:"+driver.getTitle()+" "+ListOflinks.size());
        // 5.Loop through all6.Print out how many link is missing textTOTAL
        // 7.Print out how many link has textTOTAL
        // 8.Print out how many total linkTO













    }
}

 */
